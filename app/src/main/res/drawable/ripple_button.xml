<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#2196F3"> <!-- #331E88E5 blu sfumato con opacità -->

    <!-- Maschera per il ripple -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#FFFFFF" />
        </shape>
    </item>

    <!-- Sfondo con selector -->
    <item android:id="@android:id/background" android:drawable="@drawable/button_selector" />
</ripple>

